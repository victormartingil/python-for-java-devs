# 03 — Exercises: functions & decorators

"Make the test pass" practice for [module 03](../README.md). TODO stubs + tests that fail until the implementation is right.

```bash
uv run pytest 03-functions-and-decorators/exercises -v
```

| Exercise | Stub | Goal |
|---|---|---|
| 01 | `ex03_cached.py` | Write a `@cached` memoization decorator from scratch (≈ hand-rolled AOP) |
| 02 | `ex03_mutable_default.py` | Defuse the mutable-default-argument trap |
| 03 | `ex03_forwarding.py` | Forward an arbitrary signature with `*args`/`**kwargs` |

**Don't read `solutions/` until your tests pass — the tests are the spec.** Solutions are reference implementations, not the only correct answer.
