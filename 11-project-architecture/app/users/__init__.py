"""users — a second domain module: registration and JWT login.

Same internal structure as tasks/: router, schemas, service, repository, models.
Scaling this app = adding more folders like this one, not more layers.
"""
