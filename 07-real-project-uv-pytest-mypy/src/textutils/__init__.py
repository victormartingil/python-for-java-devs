"""textutils — a tiny library used to demonstrate real project structure.

The `src/` layout + package + __init__.py + tests pattern from module 07.
"""

from textutils.core import slugify, truncate, word_count

__all__ = ["slugify", "truncate", "word_count"]
